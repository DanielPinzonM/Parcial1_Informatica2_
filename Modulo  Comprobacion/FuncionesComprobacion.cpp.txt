#include "FuncionesMatriz.h"

bool ComprobacionDeCerradura(unsigned short int ***Cerradura,unsigned short int Fila, unsigned short Columna,unsigned short *Arreglo_Comparaciones)
{
    for(unsigned short int i;i<7;i++) //Se necesita cauntas matrices componen la cerradura
    {
        
    }



    return false;
}


bool Abrecerradura(unsigned short int ***Cerradura,unsigned short int Fila, unsigned short Columna,short int *Arreglo_Comparaciones)
{
    int Dimension_Matriz_Primera, Dimension_Matriz_Segunda;
    for(int i=0;  i < 3; i++) //se necesita cuantas matrices componen la cerradura y se lo remplaza por el 3, se empieza desde 0 ya que en la prueba son 4 pero como comienza desde 0
    {
        
        /*
            Dimension_Matriz_Primera=ObtenerDimension(Cerradura[3-2-i]);      //se necesita cuantas matrices componen la cerradura y se lo remplaza por el 3
            Dimension_Matriz_Segunda=ObtenerDimension(Cerradura[3-1-i]);    //se obtienen 2 matrices para compararlas
            */
        
        if (Arreglo_Comparaciones[3-1-i] == 1)                  //se necesita saber el largo del Arreglo de comparaciones y se lo remplaza por el 3
        {
            if(ObtenerDimension(Cerradura[3-2-i]) == ObtenerDimension(Cerradura[3-1-i])) // si la dimenension no es igual retorna falso
            {
                if(ObtenerEstadoRotacion(Cerradura[3-2-i])==ObtenerEstadoRotacion(Cerradura[3-1-i]))    //si la posicion no es igual retorna falso
                {
                    if(Cerradura[3-2-i][Fila][Columna] > Cerradura[3-1-i][Fila][Columna])
                    {    
                    }
                    else
                    {
                        return false;
                    }
                }
                else // que se hace en el caso que el estado de rotacion no sea el mismo pero la dimension si es igual
                {
                    
                }
            }
            else        // que se hace si las dimensiones no son iguales
            {
                if(ObtenerEstadoRotacion(Cerradura[3-2-i])==ObtenerEstadoRotacion(Cerradura[3-1-i]))    //si la posicion no es igual retorna falso
                {
                }
                else // que se hace en el caso que el estado de rotacion no sea el mismo
                {
                  
                }
            }
               
        }
         if (Arreglo_Comparaciones[3-1-i]== 0)
        {

            if(ObtenerDimension(Cerradura[3-2-i]) == ObtenerDimension(Cerradura[3-1-i])) // si la dimenension no es igual retorna falso
            {
            if(ObtenerEstadoRotacion(Cerradura[3-2-i])==ObtenerEstadoRotacion(Cerradura[3-1-i]))    //si la posicion no es igual retorna falso
                {
                }
            else
            {
                return false;
            }
            }
            else
            {
                return false;
            }    
           // se compara lo que se hace en el caso 0 
        }
          if (Arreglo_Comparaciones[3-1-i] == -1)
        {
            //se compara lo que se hace en el caso -1
        }
          
    }
    return true;
}
